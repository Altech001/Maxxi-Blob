# Database
# Use your Neon/Postgres connection string in production.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require

# Set this locally when you want startup to fail instead of using maxxi.db.
MAXXI_REQUIRE_DATABASE_URL=false
